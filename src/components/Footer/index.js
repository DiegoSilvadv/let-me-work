
import { StyledFooter } from './styles.js';

export default function Footer () {
    return (
        <StyledFooter>
            <strong>-Desenvolvido por Diego Silva-</strong>
        </StyledFooter>
    )
}